# Поведенческая матрица сериалов: ситуации и реакции

> **Метод — перенос ЛОГИКИ, не кода.** Рефакторинг сделан потому, что
> легаси был необслуживаемым; задача не скопировать его действия, а
> перенести их смысл. Для каждого действия легаси решаем: нужно ли оно в
> новой логике, и если да — как правильно. Пользователь имеет право на
> ЛЮБОЕ, даже бессмысленное действие с сериалом — алгоритм обязан его
> **заметить** и **правильно отреагировать**.
>
> **Этот документ обязателен к исполнению.** По каждой ситуации:
> симуляция → что алгоритм видит → как реагирует → ВЕРНО или ДЫРА → при
> дыре правка отдельным коммитом со ссылкой на ID ситуации; статус в
> таблице обновляется.

## Метод симуляции

Алгоритм скана/конвейера зависит от внешнего мира РОВНО через:
- ответ `sources.parse {url}` (релизы трекера) / `sources.vk.scan` (видео VK);
- содержимое `.torrent` (`sources.torrent_file.get`) → infohash в qB;
- состояние qBittorrent (фейковый `FakeQbt` или живой qB);
- состояние БД (активные строки, кандидаты, задачи всех видов);
- наличие/состояние файлов на диске.

Любая ситуация = детерминированная комбинация этих входов. Симуляция —
прогон **реального кода** `scan`/`torrents`/`downloads` на контролируемых
входах: харнесс `tests/test_scan_module.py` / `tests/test_torrents_pipeline.py`
(фейковые `sources`/`qBit`/диск) + живые e2e-прогоны на стенде, где нужна
физика qB/yt-dlp/ffmpeg/диска.

Активные стадии торрент-конвейера (параметр для C-ситуаций):
**метаданные (magnet) → пауза → переименование → recheck → загрузка (resume).**

**Статусы:** `☐` не проверено · `✓` верно · `⚠` дыра (с фиксом) · `❓` нужно решение пользователя.

---

# ЧАСТЬ 1. ТОРРЕНТ-СЕРИАЛ (зафиксировано)

## U — действия пользователя в приложении

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| U1 | Добавил сериал (раздачи + профиль + качество) | Раздачи в реестр (`is_active`, без qB); авто-скан не запускается | ✓ | `add_registered_rows` (аудит Д1) |
| U2 | Добавил сериал без профиля правил | Скан явно «нет профиля», не падает молча | ✓ | `scan/module.py:279` raise |
| U3 | Добавил сериал без раздач | Скан: «нет раздач», корректно | ✓ | пустой план → tasks_created=0 |
| U4 | Нажал «Сканировать» | Реконсиляция БД↔qB↔диск + замена + тост | ✓ | закрыто R1/R2/P10 + тост |
| U5 | Переключил авто-скан | Расписание по `auto_scan_enabled`; выкл = не сканирует | ✓ | scheduler (аудит Д1) |
| U6 | Открыл модалку статуса | `fs.verify` + актуальный статус | ✓ | `api_series.py:200` |
| U7 | Открыл «Композицию» | Превью имён + наличие файлов; БЕЗ закачки | ✓ | композиция не шлёт dispatch (аудит Д3) |
| U8 | Сменил имя / EN-имя и сохранил | Переобработка имён (reprocess) | ✓ | `renaming.reprocess` (аудит) |
| U9 | Сменил профиль правил | Reprocess по новому профилю | ✓ | `renaming.reprocess` |
| U10 | Изменил фильтр качества | Следующий скан фильтрует; активные иного качества — до замены | ◐ | приемлемо; не верифицировано глубоко |
| U11 | Сменил `save_path` | Перемещение медиатеки (relocate) | ✓ | `test_library_relocation` |
| U12 | Изменил игнорируемые сезоны | Исключение из плана/учёта | ◐ | отражение для торрентов не верифицировано |
| U13 | Удалил сериал, «убрать из qB» ВКЛ | Каскад БД + записи qB; **файлы на диске НИКОГДА не трогаем** | ✓ | подтверждено (реш. #2); in-flight — см. C5 |
| U14 | Удалил сериал, «убрать из qB» ВЫКЛ | Каскад БД; раздача в qB и файлы остаются | ✓ | подтверждено (реш. #2) |
| U15 | force_replace (debug) + скан | Деактивировать все активные + добавить заново | ✓ | force-путь (тесты scan) |
| U16 | Скан, когда скан уже идёт | «Уже запущен», не дублировать | ✓ | `_running` guard (`module.py:273`) |

## Q — действия напрямую в qBittorrent (вне приложения)

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| Q1 | Удалил торрент из qB, файлы оставил | Заметить отсутствие → усыновить файлы на диске / вернуть к скачиванию | ✓ | re-add→конвейер→recheck реклеймит файлы (R2 `84951c8`) |
| Q2 | Удалил торрент из qB + файлы | Заметить → добавить заново → полная закачка | ✓ | re-add→конвейер→докачка (R2 `84951c8`) |
| Q3 | Пауза вручную | Мониторинг отражает; если пауза+неполный — `drive_incomplete` (P10) переподнимет на скане | ✓ | мониторинг + P10 |
| Q4 | Снял с паузы вручную до переименования | Инвариант: немедленная `force_pause` обратно | ✓ | `test_pause_stage_forces_pause_if_active` |
| Q5 | Переименовал файлы в qB вручную | Расхождение renamed_path → fs.verify → missing → recheck | ◐ | через fs.verify (edge ручного rename в qB) |
| Q6 | Сменил расположение торрента вручную | Файл не по пути → fs.verify → missing → recheck | ◐ | через fs.verify |
| Q7 | Запустил recheck вручную | Мониторинг отражает; конфликта с конвейером нет | ✓ | мониторинг |
| Q9 | Исключил/сменил приоритет файлов | Прогресс/набор отражается мониторингом | ✓ | мониторинг прогресса |

## F — файловая система (вне приложения)

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| F1 | Ожидаемого файла нет по пути (удалён/переименован/перемещён) | `fs.verify`: missing → recheck (восстановление) | ✓ | скан зовёт fs.verify→recheck (R1 `c2133cb`) |
| F2 | Удалил всю папку сериала | Все файлы missing → recheck/re-add | ✓ | через fs.verify на скане (R1 `c2133cb`) |
| F6 | Файл повреждён/обрезан | recheck в qB ловит несошедшиеся куски → докачка | ✓ | recheck |
| F7 | Путь/NAS недоступен | НЕ помечать ложно missing; сверка пропускается до восстановления | ✓ | гард в fs.verify/fs.sync (`test_sim_F7…`) |
| F8 | Нет места на диске | Ошибка записи → qB error state → флаг error (не молча) | ✓ | P5/error-флаг |
| F9 | Нет прав на запись | то же → флаг error | ✓ | P5/error-флаг |
| F10 | Файл вернулся на место | missing → renamed (самовосстановление) | ✓ | fs.verify (`module.py:489-492`) |

## T — изменения на трекере (видны на скане)

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| T1 | Нет изменений (та же дата) | Skip, `tasks_created=0`, qB не трогаем | ✓ | `test_unchanged_release_is_skipped` |
| T2 | Новый релиз: новая дата, новый контент (новый infohash) | Деактивировать+удалить старую из qB, новую в конвейер | ✓ | `test_rolling_replace`, `test_replace_deactivates_old` |
| T3 | Перевыкладка: новая дата, тот же infohash | Без дубля, без закачки, лишь обновить ярлык/дату | ✓ | `test_same_hash_relist…` |
| T4 | Релиз пропал с трекера | Активную раздачу не трогаем (она в qB, файлы на диске) | ✓ | `test_sim_T4_release_vanished_keeps_active` |
| T5 | Изменился набор качеств | Берём подходящее по фильтру серии | ✓ | `test_quality_filter` |
| T6 | Несколько релизов сразу (fixed-режим) | Замена по совпадению `episodes` | ✓ | `test_fixed_mode_replaces_by_episodes` |
| T7 | Трекер недоступен / ошибка парсинга | Зеркала, иначе громкая ошибка со списком попыток | ✓ | `sources._parse_with_mirrors` (R7 снят — фоллбэк в sources) |
| T8 | Истекла авторизация трекера | Ошибка через trackerauth.fetch → носитель error скана, не молчит | ✓ | путь ошибки в `_run_series` (носитель error, Р-11) |
| T9 | Раздача «битая» (кривые имена файлов) | Переименование/recheck это переживают | ✓ | drive→RENAMING выправил `.mkv.mkv.mkv`→чистые (живой прогон «Путешествие») |

## P — состояния qBittorrent в ходе конвейера/мониторинга

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| P1 | Идёт загрузка | Статус `downloading`, прогресс | ✓ | `test_monitor_progress…`, `test_file_torrent_full_pipeline` |
| P2 | Загрузка завершилась (100%) | Стадия → готово, перемещение/завершение | ✓ | `test_monitor_progress…` (ready) |
| P3 | Простой — нет пиров (stalledDL) | Статус `idle`, не «загрузка» | ✓ | `test_sim_P3_P4_P6…` |
| P4 | В очереди qB (queuedDL) | Статус `queued` | ✓ | `test_sim_P3_P4_P6…` |
| P5 | Ошибка раздачи (неверный путь) | Флаг `error` + текст, не «загрузка» | ✓ | `test_qbit_error_state…` |
| P6 | Файлы недоступны (missingFiles) | Флаг `error`, recheck/восстановление | ✓ | `test_sim_P3_P4_P6…` |
| P7 | Торрент исчез из qB посреди конвейера | Снять задачу + сбросить `is_active` | ✓ | `_drop_task` деактивирует строку (R3 `c2133cb`), симуляция |
| P8 | Magnet: метаданные не приходят | Стоит на стадии «метаданные» (флаг виден), не молча; таймаута нет — как легаси | ✓ | `test_magnet_runs_only_until_metadata` |
| P9 | Recheck нашёл частично/полностью | Докачать недостающее / завершить | ✓ | `test_rename_recheck_activate_flow`, full_pipeline |
| P10 | Активная раздача в qB застряла (пауза, не докачана, без задачи) или переименование не применилось | `drive_incomplete`: загнать в конвейер заново; переименование переотрабатывается | ✓ | `c2133cb`; живой прогон «Путешествие»: расстопорено, имена выправлены |

## L — жизненный цикл процесса

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| L1 | Рестарт посреди скана (добавление раздач) | Возобновить с места по `results_data` | ✓ | `scan.on_start`→`_resume_task` (аудит Д5) |
| L2 | Рестарт посреди загрузки | Восстановить задачу, сверить с qB | ✓ | `test_sim_L3…` |
| L3 | Рестарт посреди стадии конвейера | Восстановить стадию из БД, продолжить | ✓ | `test_sim_L3_pipeline_task_recovered_on_start` |
| L4 | Рестарт посреди нарезки | `slicing`→`pending`, дорезать по `progress_chapters` | ✓ | `slicing.requeue_interrupted` (аудит Д5) |
| L5 | Рестарт посреди переименования | `in_progress`→перевыполнить идемпотентно | ✓ | `renaming.unfinished` (аудит Д5) |
| L6 | Рестарт посреди перемещения | `unfinished`→доперемещение | ✓ | `library.on_start`→`unfinished` (аудит Д5) |
| L7 | qB недоступен на старте | Reconcile пропустить, данные не терять | ✓ | `_reconcile_with_qbt` skip on QbtError; `test_reconcile…` |
| L8 | Приложение убито (kill -9) | Подъём из БД, реконсиляция | ✓ | те же механизмы L1–L7 (задачи персистентны) |

## C — комбинации/гонки

| ID | Ситуация | Целевая реакция | Статус | Примечание |
|---|---|---|---|---|
| C1 | Удалил торрент из qB И запустил скан | Заметить отсутствие → re-add + конвейер | ✓ | R1+R2+R3+P10 (`84951c8`,`c2133cb`) |
| C2 | Удалил файл И открыл модалку | `fs.verify` → missing → recheck | ✓ | `api_series.py:200` → fs.verify |
| C3 | Сменил профиль И скан (рассинхрон имён) | Reprocess имён ДО скана (`_reprocess_names` первым) → сходятся | ✓ | `scan/module.py:285` reprocess до плана |
| C4 | Сменил `save_path` во время активной загрузки | Перемещение через set_location (qB сам двигает); конвейер работает qB-относительно | ◐ | низкий риск; полная гонка не верифицирована |
| C5 | Удалил сериал во время загрузки/нарезки | Прервать in-flight (yt-dlp/ffmpeg/перемещение) + снести **недоделанные** полуфайлы; законченные не трогать (реш. #3) | ✓ | yt-dlp + ffmpeg + move (тесты C5 downloads/slicing) |
| C6 | Авто-скан и ручной одновременно | Сериализация, «уже запущен» | ✓ | `_running` guard (`module.py:273`) |
| C7 | Новый релиз пришёл, пока старый ещё качается | Замена: старый убран из qB, задача снята, новый доходит | ✓ | `test_sim_C7_new_release_while_old_in_pipeline` |
| C8 | Открыл «Композицию» во время активной обработки | Превью только читает (os.path.exists/preview), мутаций конвейера нет | ✓ | `on_composition` read-only |
| C9 | Сохранил свойства во время активной обработки | reprocess и конвейерное переименование делят per-series лок — параллельно одни файлы не трогают (был баг → вероятный корень кривых имён) | ✓ | `test_sim_C9…`; фикс лока в `on_process_torrent` |

---

## Открытые дыры Части 1 (диагностированы, к фиксу)

| # | Дыра | Диагноз | План |
|---|---|---|---|
| Д1 | ✓ ЗАКРЫТА (реш. #1) | Было: SSE-дельта несла только `{id, statuses, is_busy}`, счётчик не уезжал | `series.downloaded.changed` от torrents/downloads → `series_updated`-дельта с `downloaded_episodes_count`. Доказано симуляцией + живым SSE (серия 9 → count 44). Коммит за Д1 |
| Д2 | ✓ ЗАКРЫТА (реш. #3, C5) | downloads (yt-dlp), slicing (ffmpeg kill + снос недоделанного), library (отмена перемещения) — все отменяют in-flight на `series.deleted`; законченные файлы не трогаем | Доказано тестами C5 (downloads + slicing). Коммиты за Д2 |

# ЧАСТЬ 2. VK-СЕРИАЛ (зафиксировано)

> VK без qBittorrent: источник — VK API (режимы `search`/`get_all`),
> загрузка — yt-dlp (две фазы: download → remux), компиляции режутся
> (slicing). Усыновление сопоставляет план с файлами на диске по
> переисчисленному имени. Категории — по аналогии с торрентом.

## VU — действия пользователя в приложении
| ID | Ситуация |
|---|---|
| VU1 | Добавил VK-сериал (URL канала\|запрос + профиль + приоритет качества + режим поиска) |
| VU2 | Добавил без профиля |
| VU3 | Ручной скан |
| VU4 | Переключил авто-скан |
| VU5 | Открыл «Композицию» (быстрый поиск → скрейп + усыновление) |
| VU6 | Открыл модалку статуса |
| VU7 | Сменил имя |
| VU8 | Сменил профиль правил |
| VU9 | Сменил приоритет качества VK |
| VU10 | Сменил `save_path` |
| VU11 | Изменил игнорируемые сезоны |
| VU12 | Сменил режим поиска (search/get_all) |
| VU13 | Пометил эпизод «игнорировать» / снял игнор |
| VU14 | Удалил сериал (без qB — диалог без галочки) |
| VU15 | Скан во время скана |

## VF — файловая система (.mp4 + нарезанные дети)
| ID | Ситуация | Реакция / механизм | Статус |
|---|---|---|---|
| VF1 | Удалил скачанный .mp4 | Скан → `downloads.fs.sync` сбрасывает completed→pending → dispatch перекачивает (**R4**) | ✓ |
| VF2 | Удалил нарезанный (sliced) файл | **было: дыра** — `slicing.verify` звала только композиция, обычный скан нет (fs.sync компиляции пропускает). Теперь скан зовёт `_verify_compilations` → ребёнок→missing/completed_with_errors + исходник назад в дозагрузку (R4-близнец). + F7-гард в `_verify_item` | ✓ |
| VF3 | Удалил всю папку | как VF1 для всех файлов; гард F7 при недоступном пути | ✓ |
| VF4 | Переименовал/переместил вручную | `fs.sync`: старого имени нет → lost→pending; новое имя ≠ ожидаемого → не усыновляется → перекачка | ☐ |
| VF5 | Частичный/битый .mp4 | yt-dlp атомарен: `os.replace(tmp,final)` только при rc==0 (`ytdlp.py:257`) — битый не попадает в completed | ✓ |
| VF6 | Остался мусор `.download`/`.part`/`.remux` | ytdlp finally (фаза remux) + `_remove_partial` — **дыра шва закрыта**: при обрыве в фазе download `.download.*`/`.remux.mp4` не чистились (паттерны не совпадали); `_remove_partial` теперь знает реальные шаблоны | ✓ |
| VF7 | Путь/NAS недоступен | гард в `downloads.on_fs_sync` — не сбрасывать ложно (`test_sim_F7`) | ✓ |
| VF8 | Файл вернулся на место | `_adopt_existing_files` усыновляет обратно | ☐ |

## VS — изменения источника VK (на скане)
| ID | Ситуация |
|---|---|
| VS1 | Нет изменений (те же видео) → ничего |
| VS2 | Появился новый эпизод (added) → план + загрузка |
| VS3 | Эпизод исчез из выдачи (фантом) → правило фантома (удалить девственный / оставить) |
| VS4 | Перезалив: тот же эпизод, изменился title (voiceover/эпизод переизвлеклись) |
| VS5 | Появилась компиляция, покрывающая одиночки → одиночки `replaced` |
| VS6 | Несколько качеств одного эпизода → приоритет качества |
| VS7 | VK вернул пусто / ошибка |
| VS8 | Истёк токен VK |
| VS9 | Быстрый (search) vs полный (get_all) — разный охват |

## VP — состояния загрузки/обработки
| ID | Ситуация | Реакция / механизм | Статус |
|---|---|---|---|
| VP1 | yt-dlp качает | `on_progress` → throttled `update_progress` + `downloads.queue.changed` (SSE) | ✓ |
| VP2 | yt-dlp завершил | `ok=True` строго после `os.replace` → `register_downloaded` (completed) | ✓ |
| VP3 | yt-dlp упал (96% без файла) | rc==0 + нет `.download.*` → явная ошибка «не оставил файл» (`ytdlp.py:201`), НЕ молча; item→error, ретрай сканом; мусор чистится (VF6). Живой репро — отложен | ◐ |
| VP4 | Усыновление: файл уже на диске → completed без закачки | `_adopt_existing_files` + идемпотентность ytdlp («файл уже существует») | ✓ |
| VP5 | Рассинхрон `voiceover_tag` → имя ≠ на диске → усыновление мимо | `renaming.reprocess` ДО скана переименовывает диск к актуальному имени → усыновление сходится — **проверить** | ☐ |
| VP6 | Нарезка компиляции по главам | slicing по главам (тесты есть) | ✓ |
| VP7 | Нарезка упала / частичный выход | **было: дыра** — `run_ffmpeg` писал прямо в финал, упавшая глава оставляла битый ребёнок, `_init_progress` усыновлял его как готовый. Теперь tmp→`os.replace` при rc==0 (находка 50), при неуспехе tmp снесён → партиал не появляется | ✓ |
| VP8 | Два этапа: download → remux (ffmpeg) | явные `_phase_download`/`_phase_remux`, tmp+atomic | ✓ |

## VL — жизненный цикл процесса
| ID | Ситуация | Реакция / механизм | Статус |
|---|---|---|---|
| VL1 | Рестарт посреди загрузки | `downloads.on_start` → `requeue_interrupted` (downloading/processing→pending, error не трогаем) → `_pump` | ✓ |
| VL2 | Рестарт посреди нарезки | `slicing.on_start` → `requeue_interrupted` (slicing→pending) + атомарность: оборванный ребёнок под tmp, не усыновляется (VP7) | ✓ |
| VL3 | Рестарт посреди переименования | `renaming.on_start` перевыполняет незавершённые; идемпотентно (уже переименованное пропускается) | ✓ |
| VL4 | Рестарт посреди перемещения | `library.on_start` возобновляет незавершённые; `os.rename` идемпотентен (перемещённое пропускается), один FS | ✓ |
| VL5 | Приложение убито (kill -9) | сумма VL1–VL4: все модули реконсилят в `on_start`; единственный риск порчи (битый нарезанный ребёнок) снят атомарностью | ✓ |

## VC — комбинации/гонки
| ID | Ситуация | Реакция / механизм | Статус |
|---|---|---|---|
| VC1 | Удалил .mp4 И скан | = VF1: скан зовёт `fs.sync` → перекачка (**R4**) | ✓ |
| VC2 | Сменил профиль И скан (рассинхрон имён → усыновление мимо) | `renaming.reprocess` ДО скана переименовывает на диске → имена сходятся | ☐ |
| VC3 | Открыл «Композицию» во время загрузки/нарезки | усыновление ≠ загрузка; задачи не дублируются | ☐ |
| VC4 | Сохранил свойства во время загрузки/нарезки | НЕ C9-гонка: `_reprocess_vk` трогает только `status==completed` (строки 172-176) → in-flight загрузку пропускает; единственный ренеймер на диске — он сам, под `_locks[series_id]`; `completed` ставится строго ПОСЛЕ атомарного `os.replace`; yt-dlp/ffmpeg — писатели, не ренеймеры. Устаревшее имя самозалечивается следующим reprocess | ✓ |
| VC5 | Сменил `save_path` во время загрузки | set_location/перемещение координируется | ☐ |
| VC6 | Удалил сериал во время загрузки/нарезки | Д2: отмена in-flight + снос полуфайлов | ✓ |
| VC7 | Авто-скан и ручной одновременно | `_running` отбивает повторный скан серии | ☐ |
| VC8 | Новый эпизод, пока старый качается | `_pump` параллелит с лимитом | ☐ |

---

# ПЛАН ПРОДОЛЖЕНИЯ (точка возобновления после сжатия контекста)

## Где мы сейчас
- **Торрент-матрица (Часть 1) — ЗАКРЫТА**: 62 ✓, 5 ◐ (приемлемые edge), 0 нерешённых.
- **VK-матрица (Часть 2) — НЕ начата** (все ☐), кроме затронутого фиксами Д1/Д2/F7.
- Деплой: код на стенде, systemd `series-tracker`, перезапущен; **210 passed** (4 «красных» — документированные golden/прод; иногда +1 флака teardown — см. ниже).
- Диапазон коммитов этой работы: `95c7259` (матрица) … `1161927` (Часть 1 закрыта). Ключевые фиксы: R2 `84951c8`, R1/R3/P10 `c2133cb`, Д1 `5c7e6bc`, Д2 `afe06dc`+`58bf7b5`, F7/C7 `29ec2c8`, C9 `27d7a71`.

## Метод (неизменный)
По каждой ситуации: **симуляция реального кода (фейк sources/qBit/yt-dlp/ffmpeg) → что алгоритм видит → реакция → ВЕРНО или ДЫРА → если дыра, фикс отдельным коммитом → статус в этой таблице**. Где нужна физика — живой прогон на стенде. Перенос ЛОГИКИ, не кода.

## ГЛАВНОЕ: незакрытая реальная дыра в VK — ✅ ЗАКРЫТА (R4)
- **R4 — скан не зовёт `downloads.fs.sync` для VK** (аналог R1 для торрентов). Путь скана `_run_series → _scan_vk → publish scan.plan.updated → downloads.on_plan_updated` делал только усыновление, а `on_dispatch` создавал задачи, но **проверку пропавших completed-файлов (`on_fs_sync`) не звал**. Значит «удалил VK-файл → обычный скан не замечал, не перекачивал».
- **Сделано**: в `modules/scan/module.py::_run_series` (VK-ветка) перед `_scan_vk` добавлен `await downloads.fs.sync {series_id}` под гардом BusRequestError — симметрично торрент-ветке (`torrents.fs.verify`). Гард недоступного пути (F7/VF7) — внутри `downloads.on_fs_sync`. Тест: `test_sim_R4_vk_scan_syncs_disk` (скан зовёт fs.sync); поведение самой сверки — `test_fs_sync_detects_lost_and_adopted`. Закрыты: **VF1, VF3, VC1**.

## Отложенное расследование
- **VP3 — yt-dlp падает «96% без файла»** на реально недостающем эпизоде + оставляет `.download.mp4`-мусор. Нужен живой реально-недостающий эпизод для воспроизведения (предлагал ранее «когда скажешь»). Проверить: почему yt-dlp не отдаёт файл и чистится ли temp (Д2 `_remove_partial` + ytdlp finally — проверить покрытие именно `.download.<ext>`/`.remux.mp4`).

## VK-кластеры — что верифицировать (с ожидаемым механизмом и где смотреть)
- **VU1–VU15** (действия в приложении): аналог торрент-U. Особо: VU5 (композиция быстрого поиска → скрейп+усыновление, контракт уже разбирался), VU12 (search/get_all), VU13 (игнор эпизода → `scan.item.set_ignored` → plan → усыновление). Смотреть `scan/module.py::_scan_vk`, `downloads`.
- **VF1–VF8** (файлы): VF1/VF3/VF4 → `downloads.on_fs_sync` reset→pending (после R4-фикса — и на скане). VF2 (sliced) → `slicing.on_verify` → completed_with_errors + возврат исходника. VF5 (битый .mp4) → yt-dlp атомарен (`os.replace` только при успехе). VF6 (мусор) → ytdlp finally + Д2. VF7 → гард F7 (✓). VF8 → усыновление.
- **VS1–VS9** (источник): VS1/VS2/VS3/VS5 уже косвенно в `test_scan_module` (changed, фантомы, компиляция покрывает одиночки). VS6 (качества) → `build_plan` приоритет. VS4 (перезалив, сменился title → voiceover/эпизод переизвлеклись) — см. VP5. VS7/VS8 (пусто/токен) → ошибка не молча.
- **VP1–VP8**: VP4 усыновление (тесты есть). **VP5 — рассинхрон voiceover_tag → ожидаемое имя ≠ имя на диске → усыновление мимо**: митигируется `renaming._reprocess_vk` (mass_vk_reprocess) ДО скана; проверить, что reprocess реально переименовывает на диске до усыновления и имена сходятся. VP6/VP7 нарезка (тесты есть). VP8 download→remux (parse-тесты есть).
- **VL1–VL5** (рестарт): downloads `requeue_interrupted` (downloading/processing→pending, error не трогаем), slicing/renaming/library on_start — аудит Д5 подтвердил. Прогнать симуляцию восстановления VK-загрузки.
- **VC1** = R4 (см. выше). **VC4** (save во время загрузки/нарезки) — VK-аналог C9: проверить, не гонится ли `_reprocess_vk` (rename на диске) с идущей yt-dlp-загрузкой/нарезкой; downloads/slicing — отдельные модули, общего лока с renaming нет → **потенциальная гонка, проверить**. VC5 (удаление) Д2 ✓. VC6 (_running) ✓. VC7 (новый эпизод пока старый качается) — `_pump` параллелит, проверить.

## Торрент-хвосты ◐ (приемлемые, при желании добить)
- **Q5/Q6** — ручное переименование/перемещение файлов в qB: лечится через fs.verify→recheck, но не симулировано.
- **U10** — смена фильтра качества: активные иного качества остаются до замены.
- **U12** — игнор-сезоны: отражение для торрентов не верифицировано.
- **C4** — relocate во время загрузки: низкий риск (set_location координирует), полная гонка не доказана.

## Флака тестов (не регресс)
Иногда полный прогон даёт 3-ю ошибку на teardown (`scan/repository.py:65 error_tasks`) — фоновая корутина интеграционного теста с живым qBit (`test_torrents_integration`, CLAUDE.md помечает как state-зависимый). Мои `await` в скане расширили окно. Не функциональный регресс; при желании укрепить teardown того теста.
