# Ядро новой системы (этап 1). Зависимости добавляются по этапам ТЗ
# (docs/refactoring_bus_plan.md) — не впрок.
fastapi==0.115.12
uvicorn[standard]==0.34.2
SQLAlchemy==2.0.41
alembic==1.16.1
httpx==0.28.1
requests==2.32.3
# Аутентификация веб-интерфейса (docs/security.md, Этап 1):
# argon2 — хэш пароля админа; itsdangerous — подпись сессионной куки.
argon2-cffi==25.1.0
itsdangerous==2.2.0
cloudscraper==1.2.71
beautifulsoup4==4.12.3
lxml==6.0.0
# Браузерная доставка astar/anilibria_tv (этап 6, Р-9): требует
# `playwright install firefox` после установки пакета.
playwright==1.52.0
